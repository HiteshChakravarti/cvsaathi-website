export interface Resume {
  id: string;
  user_id: string;
  name: string;
  experience: string;
  skills: string;
  // Add more fields as needed
}

export interface Template {
  id: string;
  title: string;
  // Add more fields as needed
} 